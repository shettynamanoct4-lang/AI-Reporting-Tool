public with sharing class CountContactsOnAccountCls {
    public static void testa(Map<Id,Contact> newContacts){
        Set<Id> accountId = new Set<Id>();
        for(Id newCons : newContacts.keySet()){
            
        }
        
    }

}